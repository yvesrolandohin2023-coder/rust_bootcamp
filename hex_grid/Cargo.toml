[package]
name = "hex_grid"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
rand = "0.8"
colored = "2.0" # Pour la "Colorful visualization" optionnelle
